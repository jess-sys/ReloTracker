{
  "name": "supermover-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute supermover --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute supermover --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "jose": "^5.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.60.0"
  }
}
